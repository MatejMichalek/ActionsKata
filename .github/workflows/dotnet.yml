name: .NET

on: [push]

jobs:
  welcome:
    runs-on: ubuntu-latest
    
    steps:
      - name: Hello message
        run: echo "Hello guys"
        
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-dotnet@v2
        with:
          dotnet-version: '6.0.x'
      - name: build
        run: dotnet build